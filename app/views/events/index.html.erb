<html>
<head>
  <title>E2Gather</title>
</head>
<body>

<h1>Listing events</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Host</th>
      <th>Location</th>
      <th>Date time</th>
<!--      <th>Ingredient list</th> -->
<!--      <th>Guest list</th> -->
      <th>Status</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.host %></td>
        <td><%= event.location %></td>
        <td><%= event.date_time %></td>
<!--        <td><%= event.ingredient_list %></td> -->
<!--        <td><%= event.guest_list %></td> -->
        <td><%= event.status %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
</html>
